vec4 xlat_main( in vec2 pos );
vec4 xlat_main( in vec2 pos ) {

    return vec4( pos, 0.00000, 0.00000);
}


void main() {
    vec4 xlat_retVal;

    xlat_retVal = xlat_main( vec2(gl_FragCoord));

    gl_FragData[0] = vec4( xlat_retVal);
}


