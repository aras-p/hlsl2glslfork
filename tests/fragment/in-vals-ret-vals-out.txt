vec4 xlat_main( in vec2 uv, in vec4 color );
vec4 xlat_main( in vec2 uv, in vec4 color ) {
    vec4 c;
    c = color;
    c.xy  += uv;
    return c;
}
void main() {
    vec4 xl_retval;
    xl_retval = xlat_main( vec2(gl_TexCoord[0]), vec4(gl_Color));
    gl_FragData[0] = vec4( xl_retval);
}
